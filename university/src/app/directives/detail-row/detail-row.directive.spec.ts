import { DetailRowDirective } from './detail-row.directive';

describe('DetailRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailRowDirective();
    expect(directive).toBeTruthy();
  });
});
